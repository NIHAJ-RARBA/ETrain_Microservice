# postgres-train.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-train
  namespace: railway-system
spec:
  serviceName: postgres-train # Required for StatefulSet network identity
  replicas: 1
  selector:
    matchLabels:
      app: postgres-train
  template:
    metadata:
      labels:
        app: postgres-train
    spec:
      containers:
        - name: postgres-train
          image: postgres:14-alpine
          env:
            - name: POSTGRES_DB
              value: "train_service_db"
            - name: POSTGRES_USER
              value: "train_admin"
            - name: POSTGRES_PASSWORD
              value: "1234"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-train-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U train_admin -d train_service_db
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates: # Dynamic PVC creation for StatefulSet
    - metadata:
        name: postgres-train-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
        # storageClassName: standard  # Uncomment if needed

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-train
  namespace: railway-system
spec:
  clusterIP: None # Headless service for StatefulSet
  selector:
    app: postgres-train
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
